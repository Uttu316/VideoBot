import {StyleSheet} from 'react-native';
import {DefaultTheme} from '../../utils/theme';

export const styles = StyleSheet.create({
  mainContainer: {
    backgroundColor: DefaultTheme.dark.colors.background,
  },
  backButton: {},
});
